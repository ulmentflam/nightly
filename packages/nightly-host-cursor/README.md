# nightly-host-cursor

[Nightly](../../README.md) host integration for **Cursor** — secondary host.
Registers a `.cursor/commands/nightly` slash command and a Background Agent
template. Sub-agent dispatch can either run inline or spin up a remote
Background Agent VM.

**Status:** Phase 0 stub. Real implementation lands in Phase 6.
