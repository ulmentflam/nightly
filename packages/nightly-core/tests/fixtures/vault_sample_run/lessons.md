# Lessons — sample run

- **First lesson with a bold prefix.** The body of the first lesson
  continues across multiple lines. The bullet parser must preserve
  the continuation lines.
- **Second lesson.** A shorter one.
- **Third lesson with code.** Sometimes a lesson mentions `code`
  or refers to a path like `packages/nightly-core/src/`.
