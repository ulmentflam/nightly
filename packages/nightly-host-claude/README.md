# nightly-host-claude

[Nightly](../../README.md) host integration for **Claude Code** — the primary
interactive host. Ships the `.claude/skills/nightly/SKILL.md` Skill, the
Task-tool sub-agent dispatcher, and the `PreToolUse` / `PostToolUse` hooks
that gate refused operations.

**Status:** Phase 0 stub. Real implementation lands in Phase 1.
