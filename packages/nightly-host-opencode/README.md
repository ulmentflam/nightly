# nightly-host-opencode

[Nightly](../../README.md) host integration for **opencode** (sst/opencode) —
primary host. Registers as a sub-agent under `.opencode/agents/nightly/` and
talks to a sidecar `opencode serve` over HTTP + SSE.

**Status:** Phase 0 stub. Real implementation lands in Phase 4.
